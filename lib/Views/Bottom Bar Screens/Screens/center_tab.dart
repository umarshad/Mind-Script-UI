import 'package:flutter/material.dart';

class CenterTab extends StatelessWidget {
  const CenterTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Center Tab'),
      ),
    );
  }
}
